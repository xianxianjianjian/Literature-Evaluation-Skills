# Literature Evaluation Plugin v1.3.0

## Release lineage

v1.3.0 is the next stable release after v1.1.0. It fully incorporates the unreleased v1.2 development milestone (translation evidence hardening, psychology/cognitive-neuroscience methods routing, statistical interpretation requirements, and DOCX hygiene) and adds the exact text-frame mirror workflow.

No standalone `v1.2.0` release tag exists or is planned; the v1.2 milestone remains preserved in Git history and documented in `docs/releases/v1.2-development.md`.

## Exact text-frame mirror

- Redefines new `FULL_MIRROR` work as `EXACT_TEXT_FRAME` by default; structural mirror layout requires an explicit user choice.
- Keeps source/output pages one-to-one and preserves all page boxes, rotation, columns, figures, tables and non-text pixels.
- Requires reviewed `text_frame_inventory.jsonl`, strict `font_map.json`, schema-v2 source/layout evidence and frame-level translated text.
- Renders Chinese with embedded Windows SimSun only, using source size through a fixed 95% floor without changing frame geometry or leading.
- Keeps an overflowing, non-uniform-background, or missing-SimSun artifact `PROVISIONAL`/`BLOCKED` instead of adding pages or silently reflowing it.

## Independent validation

- Recomputes PDF page geometry, one-to-one source mapping, replacement-frame containment, table cells, CJK font use and actual glyph sizing.
- Uses raster comparison to require zero changed pixels outside reviewed replacement frames.
- Generates `layout_diff.json`; hand-written render notes are diagnostic only.
- Keeps v1 schema packages readable as `LEGACY_STRUCTURAL` but never re-certifies them as exact mirrors.

## Inherited v1.2 milestone capabilities

- Explicit translation scope and evidence contracts for Main/SI coverage, page/object/table coverage, and rendered-page QC.
- Psychology/cognitive-neuroscience deep-reading router using design-appropriate public reporting and methods standards.
- Statistical interpretation requiring estimates, uncertainty, effect sizes, scientific meaning, and analysis-specific N.
- Deterministic DOCX metadata/comment sanitation and validation.

## Runtime and compatibility

- Exact rendering uses pypdf, pdfplumber, reportlab, Pillow, NumPy, fontTools and Poppler. Standalone CI installs `requirements-exact-mirror.txt` and Poppler.
- The SimSun font file is never committed. Missing or non-embeddable `simsun.ttc` is a hard blocker.
- Search and Zotero academic logic are unchanged; Deep Reading retains the v1.2 methods-hardening additions.

## Real-paper regression evidence

The full record is in `docs/releases/v1.3-real-paper-regression.md`. It covers private Main/SI source geometry, mixed SI page sizes, real journal columns, publisher margin furniture, supplementary line-number gutters, ruled/borderless table segmentation, typographic ligatures, representative Chinese replacement geometry, 100%–95% fitting/overflow behavior, the Windows production SimSun gate, and the final same-environment full-package acceptance run.

The final strict acceptance run used the private Sjøgård 9-page Main + 11-page SI package on Windows with the reviewed v2 frame/translation evidence and the installed `C:\Windows\Fonts\simsun.ttc`. On merged `main` commit `2779fb8bdcc660e8e70f8e88fd2f146f7955fb75`, the independent validator reported **23 passed / 0 failed**, `passed=true`, and `MAIN STRICT REAL-PAPER GATE: PASS`. The generated A PDF was visually inspected and accepted.

The copyrighted source PDFs, generated private translation artifacts, and SimSun font binary are not committed or redistributed.

## Release gate

The release-candidate and merged-main gates are complete:

- pull-request CI passed on Python 3.11, Python 3.12, and the Windows production-SimSun job;
- merged `main` CI run #216 passed on the same three jobs;
- the complete translated 20-page Main+SI package passed the strict Windows same-environment renderer and independent validator on merged `main`;
- the final A PDF was visually reviewed and accepted.

After the documentation-only release audit update, the runtime implementation remains identical to the already validated merged-main code. The `v1.3.0` tag may be created only after the final documentation commit's normal CI is green and the release audit confirms that no runtime files changed after the validated `main` commit.
